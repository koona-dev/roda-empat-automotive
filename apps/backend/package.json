{
  "name": "backend",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "module": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts --hot",
    "dev": "bun run src/index.ts --watch",
    "clean": "rm -rf dist node_modules && bun pm cache rm && bun install",
    "test": "bun test --watch",
    "build": "bun build src/index.ts --outdir dist"
  },
  "dependencies": {
    "hono": "^4.7.8",
    "dotenv": "^16.5.0",
    "cheerio": "^1.0.0",
    "puppeteer": "^24.8.2",
    "drizzle-orm": "^0.43.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.1",
    "typescript": "^5.8.3"
  }
}
